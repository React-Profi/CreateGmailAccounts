import { BrowserId } from "./types";
/**
 * Throws on new page if no timezone could be found, and no fallbacks are provided.
 */
export declare class TimezoneNotResolvedError extends Error {
    readonly browserId: BrowserId;
    constructor(browserId: BrowserId);
}
